# vim: set fenc=utf-8 ft=dosini:
# see: https://github.com/skywind3000/asynctasks.vim/wiki/Task-Config

######################################################################
# C / C++

# ----------------------------------------------------------
# Cmake

[cmake-build]
command=cmakebuild
cwd=<root>
mode=term
save=1

[cmake-run]
command=cmakebuild -t run
cwd=<root>
mode=term
save=1

# ----------------------------------------------------------
# Single file

[rcxx]
command=rcxx
mode=term
save=1

[rc]
command=rc
mode=term
save=1

######################################################################
# Rust (carogo can find root, do not need to specify cmd=<root> here)

[cargo-build]
command=cargo build
mode=term
save=1

[cargo-run]
command=cargo run
mode=term
save=1

######################################################################
