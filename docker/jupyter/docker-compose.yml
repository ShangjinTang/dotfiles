version: "3"

services:
  jupyter-service:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: jupyter
    image: jupyter:latest
    ports:
      - "8888:8888/tcp"
    volumes:
       - '~/jupyter-notebooks:/opt/notebooks'
